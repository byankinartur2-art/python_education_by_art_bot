# База уроков для бота-учителя Python

LESSONS = {
    "beginner": [  # Уровень "Новичок"
        {
            "id": 1,
            "title": "Что такое переменные?",
            "theory": "🐣 *Переменные*\n\n"
                      "Переменная — это 'коробка' с именем, где хранятся данные.\n\n"
                      "📌 *Правила именования:*\n"
                      "• Только буквы, цифры и _\n"
                      "• Не начинается с цифры\n"
                      "• Без пробелов\n\n"
                      "📌 *Пример:*\n"
                      "```python\nname = 'Анна'\nage = 25\nis_student = True\n```",
            "practice": "💪 *Задание 1.1*\n\n"
                        "Создайте переменную `city` и присвойте ей название вашего города.\n\n"
                        "Напиши решение в ответ на это сообщение.",
            "solution": "✅ *Правильное решение:*\n"
                        "```python\ncity = 'Москва'\n```\n\n"
                        "🔍 *Объяснение:*\n"
                        "Мы создали переменную `city` (это имя коробки),\n"
                        "и положили в неё строку `'Москва'`.\n\n"
                        "Кавычки говорят Python: 'Это текст, а не команда'.",
            "check": "city"
        },
        {
            "id": 2,
            "title": "Типы данных",
            "theory": "🐣 *Типы данных*\n\n"
                      "В Python есть основные типы:\n\n"
                      "🔢 *int* — целые числа: `10`, `-5`, `1000`\n"
                      "💧 *float* — дробные числа: `3.14`, `-0.5`\n"
                      "📝 *str* — строки (текст): `'Привет'`, `\"Hello\"`\n"
                      "✅ *bool* — правда/ложь: `True`, `False`\n\n"
                      "📌 *Пример:*\n"
                      "```python\na = 10        # int\nb = 3.14      # float\nc = 'Python'  # str\nd = True      # bool\n```",
            "practice": "💪 *Задание 1.2*\n\n"
                        "Создайте переменные разных типов:\n"
                        "- `price` (число с запятой) — цена товара\n"
                        "- `product` (строка) — название товара\n"
                        "- `in_stock` (логический) — есть ли в наличии\n\n"
                        "Напиши все три переменные.",
            "solution": "✅ *Правильное решение:*\n"
                        "```python\nprice = 99.99\nproduct = 'Ноутбук'\nin_stock = True\n```\n\n"
                        "🔍 *Объяснение:*\n"
                        "• `99.99` — это float, потому что есть точка\n"
                        "• `'Ноутбук'` — это str, потому что в кавычках\n"
                        "• `True` — это bool, с большой буквы!",
            "check": "price product in_stock"
        },
        {
            "id": 3,
            "title": "Условный оператор if",
            "theory": "🐣 *Условный оператор if/else*\n\n"
                      "Позволяет выполнять код ТОЛЬКО при выполнении условия.\n\n"
                      "📌 *Структура:*\n"
                      "```python\nif условие:\n    # код, если условие True\nelse:\n    # код, если условие False\n```\n\n"
                      "📌 *Пример:*\n"
                      "```python\nage = 18\nif age >= 18:\n    print('Можно голосовать')\nelse:\n    print('Подожди еще')\n```",
            "practice": "💪 *Задание 1.3*\n\n"
                        "Напишите программу, которая проверяет:\n"
                        "Если переменная `x` больше 10 — вывести 'Большое число'\n"
                        "Иначе — вывести 'Маленькое число'\n\n"
                        "Возьми `x = 15`",
            "solution": "✅ *Правильное решение:*\n"
                        "```python\nx = 15\nif x > 10:\n    print('Большое число')\nelse:\n    print('Маленькое число')\n```\n\n"
                        "🔍 *Объяснение:*\n"
                        "• `x = 15` — создаём переменную\n"
                        "• `if x > 10` — проверяем: 15 больше 10? Да!\n"
                        "• Значит выполняется код после if (print)\n"
                        "• else не срабатывает, т.к. условие True",
            "check": "if"
        }
    ],
    
    "intermediate": [  # Уровень "Средний"
        {
            "id": 1,
            "title": "Функции",
            "theory": "📚 *Функции*\n\n"
                      "Функция — это именованный блок кода, который можно вызывать многократно.\n\n"
                      "📌 *Структура:*\n"
                      "```python\ndef имя_функции(параметры):\n    # тело функции\n    return результат\n```\n\n"
                      "📌 *Пример:*\n"
                      "```python\ndef greet(name):\n    return f'Привет, {name}!'\n\nprint(greet('Анна'))  # Привет, Анна!\n```",
            "practice": "💪 *Задание 2.1*\n\n"
                        "Напишите функцию `square(x)`, которая принимает число и возвращает его квадрат.\n\n"
                        "Пример: `square(5)` должно вернуть `25`",
            "solution": "✅ *Правильное решение:*\n"
                        "```python\ndef square(x):\n    return x * x\n\n# Проверка:\nprint(square(5))  # 25\n```\n\n"
                        "🔍 *Объяснение:*\n"
                        "• `def square(x):` — объявляем функцию с параметром x\n"
                        "• `return x * x` — возвращаем x, умноженный на себя\n"
                        "• При вызове square(5): x = 5, возвращаем 5*5 = 25",
            "check": "def"
        }
    ],
    
    "advanced": [  # Уровень "Профи"
        {
            "id": 1,
            "title": "Классы и объекты",
            "theory": "🚀 *Объектно-Ориентированное Программирование*\n\n"
                      "Класс — это чертёж для создания объектов.\n\n"
                      "📌 *Структура:*\n"
                      "```python\nclass ИмяКласса:\n    def __init__(self, параметры):\n        self.свойство = параметр\n    \n    def метод(self):\n        return что-то\n```\n\n"
                      "📌 *Пример:*\n"
                      "```python\nclass Dog:\n    def __init__(self, name):\n        self.name = name\n    \n    def bark(self):\n        return f'{self.name} говорит Гав!'\n\nmy_dog = Dog('Бобик')\nprint(my_dog.bark())\n```",
            "practice": "💪 *Задание 3.1*\n\n"
                        "Создайте класс `Car` с:\n"
                        "- свойством `brand` (марка)\n"
                        "- метод `honk()`, который возвращает 'Бип-бип!'\n\n"
                        "Создайте объект `my_car = Car('Toyota')` и вызовите метод honk()",
            "solution": "✅ *Правильное решение:*\n"
                        "```python\nclass Car:\n    def __init__(self, brand):\n        self.brand = brand\n    \n    def honk(self):\n        return 'Бип-бип!'\n\nmy_car = Car('Toyota')\nprint(my_car.honk())  # Бип-бип!\n```\n\n"
                        "🔍 *Объяснение:*\n"
                        "• `__init__` — конструктор, вызывается при создании объекта\n"
                        "• `self.brand = brand` — сохраняем марку внутри объекта\n"
                        "• `honk()` — метод, который просто возвращает звук",
            "check": "class"
        }
    ]
}

# Уровни и их эмодзи
LEVELS = {
    "beginner": {"name": "🐣 Новичок", "emoji": "🐣"},
    "intermediate": {"name": "📚 Средний", "emoji": "📚"},
    "advanced": {"name": "🚀 Профи", "emoji": "🚀"}
}

# Хранилище прогресса пользователей (в реальном проекте использовать БД)
# Сейчас храним в памяти (при перезапуске бота данные теряются)
user_progress = {}  # {user_id: {"level": "beginner", "current_lesson": 1}}
